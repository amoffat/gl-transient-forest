export class Marker {
  // The slug of the marker. This is what wil be used to reference the marker in
  // your code.
  slug!: string;
  // An explanation of the marker. This is only for internal use to explain to
  // other level developers what the marker represents.
  description!: string;
}
